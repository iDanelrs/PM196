// Declaramos una variable llamada 'nombre' y le asignamos el valor "Armando"
let nombre = "Armando";

// Declaramos una constante llamada 'edad' y le asignamos el valor 25
const edad = 25;

// Cambiamos el valor de la variable 'nombre' a "Ana Maria"
nombre = "Ana Maria";

// Creamos una constante 'saludo' que concatena un mensaje usando 'nombre' y 'edad'
const saludo = "Hola, " + nombre + ". Tienes " + edad + " años.";

// Mostramos el saludo en la consola
console.log(saludo);
